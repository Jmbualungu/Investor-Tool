# ═══════════════════════════════════════════════════════════════════════════
# Investor Tool Backend - Environment Variables Template
# ═══════════════════════════════════════════════════════════════════════════
# 
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
# 
# SECURITY WARNING:
#   - NEVER commit .env to version control (it's in .gitignore)
#   - NEVER embed these keys in your iOS app
#   - iOS app should ONLY call this backend's endpoints
#   - This backend calls external APIs using these secrets
# ═══════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────
# Server Configuration
# ───────────────────────────────────────────────────────────────────────────

# Environment: development | production | test
NODE_ENV=development

# Server port (8080 for iOS development)
PORT=8080

# Logging level: debug | info | warn | error
LOG_LEVEL=info

# Enable rate limiting (recommended in production)
ENABLE_RATE_LIMITING=false

# ───────────────────────────────────────────────────────────────────────────
# Third-Party API Keys
# ───────────────────────────────────────────────────────────────────────────
# 
# IMPORTANT: These keys are optional in development but required when the
# corresponding features are used. The backend will warn on startup if keys
# are missing in development mode.
# 
# NEVER expose these to the iOS client - keep them server-side only!
# ───────────────────────────────────────────────────────────────────────────

# OpenAI API Key (for AI-powered forecasting and analysis)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Market Data API Key (for real-time stock data)
# Example providers: Alpha Vantage, Polygon.io, IEX Cloud
MARKET_DATA_API_KEY=your-market-data-api-key-here

# Financial Data API Key (for fundamentals, earnings, etc.)
# Example providers: Financial Modeling Prep, Finnhub, Yahoo Finance
FINANCIAL_API_KEY=your-financial-api-key-here

# ───────────────────────────────────────────────────────────────────────────
# Development Notes
# ───────────────────────────────────────────────────────────────────────────
# 
# After creating .env with your keys:
#   1. Run: npm install
#   2. Run: npm run dev
#   3. Verify: curl http://localhost:8080/health
# 
# For iOS Simulator: Use http://localhost:8080 or http://127.0.0.1:8080
# For Physical Device: Use http://<your-mac-ip>:8080
# 
# Find your Mac's IP: ifconfig | grep "inet " | grep -v 127.0.0.1
# ───────────────────────────────────────────────────────────────────────────
