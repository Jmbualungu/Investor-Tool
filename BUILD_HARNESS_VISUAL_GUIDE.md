# Build Harness Visual Guide

## 🎨 Debug HUD Location

```
┌─────────────────────────────────────┐
│ ┌─────────────────┐                 │ ← Simulator Screen
│ │ DEBUG HUD       │                 │
│ │ BuildStamp-001  │                 │
│ │ onboarding: seen│                 │
│ │ path: 2         │                 │
│ │ ticker: AAPL    │                 │
│ │                 │                 │
│ │ [Reset App State]                │
│ └─────────────────┘                 │
│                                     │
│                                     │
│        Main App Content             │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

**Position**: Top-left corner  
**Visibility**: DEBUG builds only  
**Interaction**: Tap "Reset App State" button

---

## 🏗️ State Flow Diagram

```
App Launch
    ↓
┌───────────────────────────────────────┐
│ ForecastAIApp (@main)                 │
│ - SwiftData modelContainer            │
└───────────────────────────────────────┘
    ↓
┌───────────────────────────────────────┐
│ AppRoot (SINGLE ENTRY POINT)          │
│                                       │
│ @StateObject flowState ← Created once │
│ @State path ← Navigation array        │
│ @AppStorage hasSeenOnboarding         │
│ @AppStorage marketingMode             │
└───────────────────────────────────────┘
    ↓
    ├──── hasSeenOnboarding == false ────┐
    │                                     ↓
    │                    ┌────────────────────────────┐
    │                    │ FirstLaunchOnboardingView  │
    │                    │ - 3 pages                  │
    │                    │ - Skip button              │
    │                    │ - Safe background          │
    │                    └────────────────────────────┘
    │                                     ↓
    │                            (User taps Continue)
    │                                     ↓
    │                         hasSeenOnboarding = true
    │                                     ↓
    └──── hasSeenOnboarding == true ─────┘
                        ↓
        ┌───────────────────────────────┐
        │ DCFTickerSearchView           │
        │ - Search companies            │
        │ - Select ticker               │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ CompanyContextView            │
        │ - Company info                │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ InvestmentLensView            │
        │ - Horizon, Style, Objective   │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ RevenueDriversView            │
        │ - Assumption sliders          │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ OperatingAssumptionsView      │
        │ - Margins, Tax, CapEx         │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ ValuationAssumptionsView      │
        │ - Discount rate, Growth       │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ ValuationResultsView          │
        │ - DCF results                 │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │ SensitivityAnalysisView       │
        │ - Scenario comparisons        │
        └───────────────────────────────┘
```

---

## 🔄 Reset Flow

```
User Taps "Reset App State" Button
            ↓
┌──────────────────────────────────────┐
│ DebugHUD.onReset() callback          │
└──────────────────────────────────────┘
            ↓
    ┌───────────────┐
    │ Reset State   │
    └───────────────┘
            ↓
    ├── hasSeenOnboarding = false
    ├── marketingMode = false
    ├── path = []
    └── flowState.resetAllToDefaults()
                ↓
        ┌───────────────────────────┐
        │ DCFFlowState.resetAll...  │
        │                           │
        │ selectedTicker = nil      │
        │ watchlistSymbols = []     │
        │ investmentLens = default  │
        │ revenueDrivers = []       │
        │ operatingAssumptions = def│
        │ valuationAssumptions = def│
        │ snapshots = cleared       │
        └───────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│ App returns to Onboarding screen     │
│ All state is fresh                   │
└──────────────────────────────────────┘
```

---

## 🔍 Build Stamp Verification

```
Code Change
    ↓
Update buildStamp in AppRoot.swift
    private let buildStamp = "BuildStamp-002"
    ↓
Build & Run (Cmd+R)
    ↓
┌─────────────────────────────────────┐
│ ┌─────────────────┐                 │
│ │ DEBUG HUD       │                 │
│ │ BuildStamp-002  │ ← Changed!      │
│ │ onboarding: seen│                 │
│ │ path: 0         │                 │
│ │ ticker: nil     │                 │
│ └─────────────────┘                 │
│                                     │
│  ✅ Simulator running latest code   │
└─────────────────────────────────────┘
```

---

## 📊 FlowState Injection Pattern

```
┌─────────────────────────────────────┐
│ AppRoot                             │
│                                     │
│ @StateObject var flowState          │ ← Created ONCE
│         ↓                           │
│   .environmentObject(flowState)     │ ← Injected
│         ↓                           │
└─────────────────────────────────────┘
          ↓
┌─────────────────────────────────────┐
│ All Child Views                     │
│                                     │
│ @EnvironmentObject var flowState    │ ← Received
│                                     │
│ DCFTickerSearchView                 │
│ CompanyContextView                  │
│ InvestmentLensView                  │
│ RevenueDriversView                  │
│ OperatingAssumptionsView            │
│ ValuationAssumptionsView            │
│ ValuationResultsView                │
│ SensitivityAnalysisView             │
└─────────────────────────────────────┘

Benefits:
✅ FlowState created once
✅ Never missing in child views
✅ No runtime crashes
✅ All views share same state
```

---

## 🗺️ Navigation Path Management

```
┌─────────────────────────────────────┐
│ AppRoot                             │
│                                     │
│ @State var path: [AppRoute] = []    │ ← Single source
│         ↓                           │
│ NavigationStack(path: $path)        │
│         ↓                           │
└─────────────────────────────────────┘
          ↓
┌─────────────────────────────────────┐
│ Navigation Actions                  │
│                                     │
│ path.append(.dcfCompanyContext)     │
│ path.append(.dcfInvestmentLens)     │
│ path.append(.dcfRevenueDrivers)     │
│ path = [] // Reset to root          │
└─────────────────────────────────────┘

Path Example:
[]                           ← Root (Ticker Search)
[.dcfCompanyContext(ticker)] ← 1 level deep
[.dcfCompanyContext(ticker), .dcfInvestmentLens] ← 2 levels

Reset path = [] → Returns to root instantly
```

---

## 🎭 DEBUG vs Release Builds

```
DEBUG BUILD (Development)
┌─────────────────────────────────────┐
│ ┌─────────────────┐                 │
│ │ DEBUG HUD       │ ← Visible       │
│ │ BuildStamp-001  │                 │
│ │ [Reset button]  │                 │
│ └─────────────────┘                 │
│                                     │
│        App Content                  │
└─────────────────────────────────────┘

RELEASE BUILD (Production)
┌─────────────────────────────────────┐
│                                     │
│                                     │ ← No HUD
│                                     │
│                                     │
│        App Content                  │
│                                     │
└─────────────────────────────────────┘

Automatic via:
#if DEBUG
  DebugHUD(...)
#endif
```

---

## 🔐 Onboarding Safety

```
BEFORE (Unsafe)
┌─────────────────────────────────────┐
│                                     │
│  Decorative Background              │
│  (might capture taps)               │
│                                     │
│  [Continue Button] ← Might not work │
└─────────────────────────────────────┘

AFTER (Safe)
┌─────────────────────────────────────┐
│ Color(.systemBackground) ← Solid    │
│      .ignoresSafeArea()             │
│                                     │
│ AbstractBackground()                │
│   .allowsHitTesting(false) ← Safe   │
│                                     │
│ [Skip] [Continue] ← Always work     │
└─────────────────────────────────────┘

Benefits:
✅ Never blank/transparent
✅ Background doesn't block taps
✅ Buttons always respond
✅ Skip button for quick testing
```

---

## 📁 File Structure

```
Investor Tool/
├── App/
│   ├── AppRoot.swift          ← NEW: Single entry point
│   ├── DebugHUD.swift         ← NEW: Debug overlay
│   ├── ForecastAIApp.swift    ← MODIFIED: Uses AppRoot
│   ├── RootAppView.swift      ← OLD: No longer used
│   └── AppRouter.swift
│
├── Core/
│   └── Models/
│       └── DCFFlowState.swift ← MODIFIED: Added resetAllToDefaults()
│
└── Features/
    └── Onboarding/
        └── FirstLaunchOnboardingView.swift ← MODIFIED: Safe background
```

---

## ⚡ Quick Actions

```
┌────────────────────────────────────────┐
│ Action          │ How                  │
├────────────────────────────────────────┤
│ Verify Update   │ Change build stamp   │
│                 │ Build & check HUD    │
├────────────────────────────────────────┤
│ Reset State     │ Tap "Reset App State"│
│                 │ in Debug HUD         │
├────────────────────────────────────────┤
│ Check State     │ Look at Debug HUD    │
│                 │ path count, ticker   │
├────────────────────────────────────────┤
│ Hide HUD        │ Build for Release    │
│                 │ (automatic)          │
└────────────────────────────────────────┘
```

---

## 🎯 Success Indicators

When running in Simulator, you should see:

✅ Debug HUD in top-left corner  
✅ Build stamp showing (e.g., "BuildStamp-001")  
✅ Onboarding status correct  
✅ Path count updates as you navigate  
✅ Ticker shows when selected  
✅ Reset button resets to onboarding  
✅ No blank screens  
✅ Navigation works smoothly  

If any of these fail, check:
- Build succeeded?
- Running DEBUG configuration?
- Simulator running latest build?
- Try changing build stamp and rebuilding

---

**Visual Guide Version**: 1.0  
**Last Updated**: January 19, 2026
